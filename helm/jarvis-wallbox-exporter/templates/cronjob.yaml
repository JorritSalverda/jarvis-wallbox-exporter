apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: {{ include "jarvis-wallbox-exporter.fullname" . }}
  labels:
    {{- include "jarvis-wallbox-exporter.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.cronjob.schedule | quote }}
  concurrencyPolicy: {{ .Values.cronjob.concurrencyPolicy }}
  failedJobsHistoryLimit: {{ .Values.cronjob.failedJobsHistoryLimit }}
  successfulJobsHistoryLimit: {{ .Values.cronjob.successfulJobsHistoryLimit }}
  suspend: false
  jobTemplate:
    spec:
      completions: 1
      parallelism: 1
      backoffLimit: 0
      activeDeadlineSeconds: 240
      ttlSecondsAfterFinished: {{ .Values.cronjob.ttlSecondsAfterFinished }}
      template:
        metadata:
          {{- with .Values.podAnnotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}  
          labels:
            {{- include "jarvis-wallbox-exporter.labels" . | nindent 12 }}
        spec:
          restartPolicy: Never
          {{- with .Values.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          serviceAccountName: {{ include "jarvis-wallbox-exporter.serviceAccountName" . }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          containers:
          - name: {{ .Chart.Name }}
            securityContext:
              {{- toYaml .Values.securityContext | nindent 14 }}
            image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
            imagePullPolicy: {{ .Values.image.pullPolicy }}    
            env:
            - name: ESTAFETTE_LOG_FORMAT
              value: {{ .Values.logFormat }}
            - name: USERNAME
              valueFrom:
                secretKeyRef:
                  key: username
                  name: {{ include "jarvis-wallbox-exporter.fullname" . }}
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: {{ include "jarvis-wallbox-exporter.fullname" . }}
            - name: NATS_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "jarvis-wallbox-exporter.fullname" . }}
                  key: nats-host
            - name: NATS_SUBJECT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "jarvis-wallbox-exporter.fullname" . }}
                  key: nats-subject
            - name: MEASUREMENT_FILE_CONFIG_MAP_NAME
              value: {{ include "jarvis-wallbox-exporter.fullname" . }}
            resources:
              {{- toYaml .Values.resources | nindent 14 }}
            volumeMounts:
            - name: configs
              mountPath: /configs
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.affinity }}
          affinity:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          terminationGracePeriodSeconds: 300
          volumes:
          - name: configs
            configMap:
              name: {{ include "jarvis-wallbox-exporter.fullname" . }}